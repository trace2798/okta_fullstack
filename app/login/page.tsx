import AuthForm from "@/components/auth-form";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <>
      <AuthForm />
    </>
  );
};

export default page;
